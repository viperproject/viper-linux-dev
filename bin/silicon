#!/bin/bash

docker run \
  --rm \
  -ti \
  -v "$(pwd):/home/developer/source" \
  -v "$(readlink -f "$1"):/home/developer/file.sil" \
  vakaras/viper-build:0.0.1 \
  /bin/bash -c "cd /home/developer/source/silicon && sbt -no-share 'run /home/developer/file.sil'"
