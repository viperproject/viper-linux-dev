#!/bin/bash

docker run -ti --rm \
  -e DISPLAY=$DISPLAY \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  -v "$(pwd):/home/developer/source" \
  -v "$(pwd)/workspace:/home/developer/workspace" \
  vakaras/viper-build:0.0.1
