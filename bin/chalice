#!/bin/bash

if [ -z "$1" ];
then
  echo "Usage: $0 <file.chalice>"
  exit 1
fi

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
CHALICE_FILE="$1"
SILVER_FILE="${CHALICE_FILE%.chalice}.sil"

"$DIR/chalice2silver" "$CHALICE_FILE" "$SILVER_FILE" && \
"$DIR/silicon" "$SILVER_FILE"
